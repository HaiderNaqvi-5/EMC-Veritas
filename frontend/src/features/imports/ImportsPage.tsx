import { ChangeEvent, useState } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { apiRequest, apiUrl } from "../../lib/api/client";

type Row = { row_number: number; roll_number: string | null; full_name: string | null; outcome: string; detail: string | null };
type Preview = { valid_rows: number; duplicate_rows: number; conflicting_rows: number; invalid_rows: number; rows: Row[] };
type Activity = { id: string; name: string; activity_date: string };

export function ImportsPage() {
  const client = useQueryClient(); const [preview, setPreview] = useState<Preview | null>(null); const [resolvedConflicts, setResolvedConflicts] = useState<Set<number>>(new Set()); const [activityId, setActivityId] = useState(""); const [exportError, setExportError] = useState<string | null>(null);
  const activities = useQuery({ queryKey: ["admin", "activities"], queryFn: () => apiRequest<Activity[]>("/admin/activities") });
  const upload = useMutation({ mutationFn: (file: File) => { const data = new FormData(); data.append("file", file); return apiRequest<Preview>("/admin/imports/students/preview", { method: "POST", body: data }); }, onSuccess: (result) => { setPreview(result); setResolvedConflicts(new Set()); } });
  const commit = useMutation({ mutationFn: () => apiRequest<{ created: number; skipped_conflicts: number }>("/admin/imports/students/commit", { method: "POST", body: JSON.stringify({ rows: preview?.rows.filter((row) => row.outcome === "valid" || (row.outcome === "conflict" && resolvedConflicts.has(row.row_number))).map((row) => ({ roll_number: row.roll_number, full_name: row.full_name, conflict_resolution: row.outcome === "conflict" ? "skip" : null })) ?? [] }) }), onSuccess: () => { void client.invalidateQueries({ queryKey: ["admin", "students"] }); setPreview(null); } });
  function selectFile(event: ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; if (file) upload.mutate(file); }
  async function exportParticipants() { if (!activityId) return; setExportError(null); try { const response = await fetch(apiUrl(`/admin/imports/activities/${activityId}/participants/export`), { credentials: "include" }); if (!response.ok) throw new Error((await response.json().catch(() => null))?.detail ?? "Export could not be generated."); const url = URL.createObjectURL(await response.blob()); const anchor = document.createElement("a"); anchor.href = url; anchor.download = "Export Participants.xlsx"; anchor.click(); URL.revokeObjectURL(url); } catch (error) { setExportError(error instanceof Error ? error.message : "Export could not be generated."); } }
  return <section><h1 className="text-3xl font-bold">Student import and participant export</h1><p className="mt-2 text-slate-600 dark:text-slate-400">Review every `.xlsx` row before committing. Existing names are never overwritten.</p><div className="mt-6 rounded-xl border p-4"><h2 className="font-semibold">Import students</h2><input className="mt-3" type="file" accept=".xlsx" onChange={selectFile}/>{upload.isPending && <p className="mt-3">Reviewing spreadsheet…</p>}{upload.isError && <p role="alert" className="mt-3 text-red-700">{upload.error.message}</p>}{preview && <div className="mt-4"><p>{preview.valid_rows} valid · {preview.duplicate_rows} duplicate · {preview.conflicting_rows} conflicting · {preview.invalid_rows} invalid</p><div className="mt-3 overflow-x-auto rounded border"><table className="w-full text-left text-sm"><thead><tr><th className="p-2">Row</th><th className="p-2">Roll</th><th className="p-2">Name</th><th className="p-2">Result</th></tr></thead><tbody>{preview.rows.map((row) => <tr key={row.row_number} className="border-t"><td className="p-2">{row.row_number}</td><td className="p-2">{row.roll_number}</td><td className="p-2">{row.full_name}</td><td className="p-2">{row.outcome}{row.detail ? `: ${row.detail}` : ""}{row.outcome === "conflict" && <label className="ml-3 text-xs"><input type="checkbox" checked={resolvedConflicts.has(row.row_number)} onChange={(event) => setResolvedConflicts((current) => { const next = new Set(current); if (event.target.checked) next.add(row.row_number); else next.delete(row.row_number); return next; })}/> Skip existing record</label>}</td></tr>)}</tbody></table></div><button disabled={commit.isPending} onClick={() => commit.mutate()} className="mt-4 rounded bg-slate-900 px-4 py-2 text-white">Commit reviewed rows</button>{commit.isError && <p role="alert" className="mt-3 text-red-700">{commit.error.message}</p>}{commit.isSuccess && <p className="mt-3 text-green-700">Import completed: {commit.data.created} students created; {commit.data.skipped_conflicts} conflicts skipped.</p>}</div>}</div><div className="mt-6 rounded-xl border p-4"><h2 className="font-semibold">Export participants</h2><p className="mt-1 text-sm text-slate-600 dark:text-slate-400">Downloads the required Roll Number, Student Name, Activity Name, Activity Date, and Eligibility Status workbook.</p><div className="mt-3 flex flex-wrap gap-3"><select value={activityId} onChange={(event) => setActivityId(event.target.value)} className="rounded border p-2"><option value="">Choose activity</option>{activities.data?.map((activity) => <option key={activity.id} value={activity.id}>{activity.name} — {activity.activity_date}</option>)}</select><button disabled={!activityId} onClick={() => void exportParticipants()} className="rounded border px-4 py-2">Download Export Participants.xlsx</button></div>{activities.isError && <p role="alert" className="mt-3 text-red-700">{activities.error.message}</p>}{exportError && <p role="alert" className="mt-3 text-red-700">{exportError}</p>}</div></section>;
}
